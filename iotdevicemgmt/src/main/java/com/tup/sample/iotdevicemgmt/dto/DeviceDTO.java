package com.tup.sample.iotdevicemgmt.dto;

// Converted into a record
// Add implementation to check all attributes are not null or empty
public record DeviceDTO(
        String name,
        String type,
        String location)
{}
